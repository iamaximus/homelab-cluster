apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: docs
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/iamaximus/docs.git
  secretRef:
    name: flux-system
  ref:
    tag: tag/production
