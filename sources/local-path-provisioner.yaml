apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: local-path-provisioner
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/iamaximus/local-path-provisioner.git
  secretRef:
    name: flux-system
  ref:
    branch: main
